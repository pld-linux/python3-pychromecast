# upstream caps the build backends at the next unreleased version; the caps carry no code change
--- pychromecast-14.0.10/pyproject.toml.orig
+++ pychromecast-14.0.10/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=65.6,<83.0", "wheel>=0.37.1,<0.47.0"]
+requires = ["setuptools>=65.6", "wheel>=0.37.1"]
 build-backend = "setuptools.build_meta"
 
 [project]
